
package projects;import java.io.File;import java.util.StringTokenizer;import java.io.FileNotFoundException;import java.util.Scanner;public class Hasan_GameOfLife {    int col, row, cell, value;int[][] board;int[][] temp;public Hasan_GameOfLife() throws FileNotFoundException{  String fname; //file nameint gen; //generationsScanner kb = new Scanner(System.in);System.out.println("enter file name:");     fname = kb.nextLine();    File file = new File(fname);Scanner inputFile = new Scanner(file);//reads the line int in the line     row = inputFile.nextInt();             col = inputFile.nextInt();           System.out.println(row);       System.out.println(col);              inputFile.nextLine();//takes its value and allocates space for it in the array     board = new int[row][col];      while(inputFile.hasNext()){      for (int i = 0; i < row; i++) {       String line = inputFile.nextLine();              StringTokenizer spaceremoval = new StringTokenizer(line, " ");              System.out.println(spaceremoval);              for(int j = 0; j < col; j++){       board[i][j] = spaceremoval.nextToken().charAt(0);       }  }}inputFile.close();System.out.println("Game board is ready.");}
public int getCell(int r, int c){if(r > row){return 0;}else if(c > col){return 0;}else return board[c][r];}public void setCell(int r, int c, int v){           char life;if(v == 0){life = '0';}else if(v == 1){life = 'X';} board[r][c] = v;        }public void computeNextGen(int gen){print(); if(gen != 0){     computeNextGen(--gen);}  }public void print(){System.out.println(row);System.out.println(col);for(int i = 0; i < row; i++){for(int j = 0; j < col; j++){//System.out.println(i);//System.out.println(j);System.out.print(board[i][j]);}System.out.println();
}}/* * begin with initial conditions: start off with a file that are the initial conditions *  * how many generations do you want to compute? *  * TESTING RECURSIONS ALGORTHIMS!! THIS IS ALL RECURSION. *  * if we get to edge of 2d array, and outside it is outside of the board * this method checks  *//* System.out 'TYPE IN THE NAME OF THE FILE YOU WANT TO USE.' *  *  *  *  *  */public static void main(String[] args) throws FileNotFoundException{   int generationnum;      Scanner kb = new Scanner(System.in);          Hasan_GameOfLife test = new Hasan_GameOfLife();   System.out.println("How many generations do you want?");      generationnum = kb.nextInt();      test.computeNextGen(generationnum);}}